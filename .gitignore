# dependencies
node_modules/

# build output
dist/
build/

# env files (rotate any leaked keys!)
.env*.local
.env

# integration secrets — API keys / webhook secret (NEVER commit)
Integration API Keys.txt
*API Keys*.txt
*api-keys*

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo

# vite
*.local

# backup folders
types.bak/

# Supabase CLI local cache — auth tokens + project link
supabase/.temp/

# Claude Code harness runtime state (transient locks — never commit)
.claude/scheduled_tasks.lock
.claude/*.lock
