-- Optional seed data — run AFTER creating your first auth user via the
-- Supabase Dashboard, then update the UUIDs below to match.

-- Promote a user to super_admin (replace <your-user-id> with the real auth.uid()):
-- update profiles set role = 'super_admin' where id = '<your-user-id>';

-- Sample clients:
-- insert into clients (party_name) values
--   ('Reliance Retail'),
--   ('Trent Westside'),
--   ('Aditya Birla Fashion');
